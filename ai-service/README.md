# AI Evaluation Service

Deferred for the first production release. The PostgreSQL `AIEvaluation` model
and the admin placeholder are already present so a future evaluation worker can
be added without changing the core event lifecycle.
